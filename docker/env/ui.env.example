# UI runtime environment example for the deployed host.
# The UI runtime should remain secret-free.

NODE_ENV=production
PY_AG_UI_URL=http://backend:8000/ag-ui/
NEXT_PUBLIC_USE_MOCK_AGENT=0
NEXT_PUBLIC_TRACE_CAPTURE_ENABLED=0

