# UI runtime environment example for the deployed ECS task.
# The UI runtime should remain secret-free.

NODE_ENV=production
APP_ENV=dev
APP_RELEASE_VERSION=0.1.0
PY_AG_UI_URL=http://replace-me-alb-dns-name/ag-ui/
NEXT_PUBLIC_USE_MOCK_AGENT=0
NEXT_PUBLIC_TRACE_CAPTURE_ENABLED=0
