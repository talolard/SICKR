# Phase 1 Task Sequence

This folder turns the dictated notes into an execution plan for Phase 1.

## Phase 1 Goal
Build a local web experience where a user asks natural-language furniture queries and receives relevant IKEA items using pre-indexed Gemini embeddings stored/queryable via DuckDB.

## Scope Decisions (Locked)
- Geography: Germany-only dataset focus for Phase 1.
- Web stack: latest Django release with server-rendered templates.
- Eval target: 200 labeled queries.
- Retrieval model: semantic search over precomputed embeddings.

## Architecture Decisions (Phase 1)
- Keep runtime split explicit:
  - `ingest/index` path is an offline pipeline (CLI jobs).
  - `web` path is request/response only and never generates catalog embeddings.
- Use a layered layout under `src/tal_maria_ikea/`:
  - `src/tal_maria_ikea/ingest/`: data shaping + embedding index jobs.
  - `src/tal_maria_ikea/retrieval/`: query embedding + candidate retrieval + ranking orchestration.
  - `src/tal_maria_ikea/web/`: Django project/app code, forms, class-based views, template rendering.
  - `src/tal_maria_ikea/shared/`: settings, logging, typed contracts used across ingest/retrieval/web.
- Keep SQL-first data operations:
  - `sql/` stores schema/build/load/query SQL.
  - Python calls SQL files; avoid large inline SQL.
- Keep evaluation isolated and repeatable:
  - `data/eval/` stores eval query sets and labels.
  - `src/tal_maria_ikea/eval/` stores metric runner and report code.
- Keep docs and specs synchronized:
  - Architecture and runbooks live in `docs/`.
  - Phase plan details live in `spec/phase1/`.

## Expected Directory Shape After Phase 1
- `src/tal_maria_ikea/ingest/`
- `src/tal_maria_ikea/retrieval/`
- `src/tal_maria_ikea/web/`
- `src/tal_maria_ikea/eval/`
- `src/tal_maria_ikea/shared/`
- `sql/`
- `data/eval/`
- `tests/ingest/`, `tests/retrieval/`, `tests/web/`, `tests/eval/`

## Work Sequence
1. [Data audit and Germany-focused modeling](1_data_and_db.md)
2. [Embedding text strategy and batch indexing job](2_embedding_pipeline.md)
3. [Semantic retrieval service over DuckDB](3_retrieval_service.md)
4. [Django web app (local only)](4_django_web_app.md)
5. [Evaluation set + retrieval quality loop](5_eval_set_and_metrics.md)
6. [Integration hardening and phase-exit checklist](6_integration_and_exit.md)

## Dependencies Across Tasks
- `2_embedding_pipeline` depends on baseline tables from `1_data_and_db`.
- `3_retrieval_service` depends on vectors generated by `2_embedding_pipeline`.
- `4_django_web_app` depends on stable query interface from `3_retrieval_service`.
- `5_eval_set_and_metrics` depends on output quality from `2` and `3`.
- `6_integration_and_exit` depends on all prior tasks.

## Subtask Location Map
- `1_data_and_db` primarily changes: `sql/`, `docs/data/`, `scripts/`
- `2_embedding_pipeline` primarily changes: `src/tal_maria_ikea/ingest/`, `sql/`, `docs/`
- `3_retrieval_service` primarily changes: `src/tal_maria_ikea/retrieval/`, `sql/`, `docs/`
- `4_django_web_app` primarily changes: `src/tal_maria_ikea/web/`, `templates/`, `docs/`
- `5_eval_set_and_metrics` primarily changes: `data/eval/`, `src/tal_maria_ikea/eval/`, `docs/`
- `6_integration_and_exit` primarily changes: `docs/`, `tests/`, top-level runbook scripts

## Out of Scope for Phase 1
- Floor-plan upload/processing.
- Apartment fit validation against geometry constraints.
- Production deployment and multi-user hosting.
