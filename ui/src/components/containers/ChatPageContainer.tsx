import type { ReactElement } from "react";

export function ChatPageContainer(): ReactElement {
  return (
    <main>
      <h1>CopilotKit UI</h1>
      <p>Container shell for chat page composition.</p>
    </main>
  );
}
