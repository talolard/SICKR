name = "epic_worker"
description = "Epic Worker. Use this role to own one epic in one worktree or assigned branch until the work is implemented, validated, PR-ready, and documented."
model_reasoning_effort = "high"
sandbox_mode = "danger-full-access"
developer_instructions = """
Before acting, read `AGENTS.md`, `.codex/agents/epic_worker/prompt.epic_worker.md`, and `.codex/prompt_support/epic_structure.md`.
Treat the prompt and support file as the execution contract for one epic.
Own readiness through validation and PR health, but do not merge your own work unless the user explicitly overrides the normal workflow.
"""
