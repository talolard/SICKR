model_reasoning_effort = "high"
sandbox_mode = "danger-full-access"
developer_instructions = """
Before acting, read `AGENTS.md` and `.codex/agents/merge_coordinator/prompt.merge_coordinator.md`.
Treat the prompt as the merge-run contract.
Land one merge-request bead at a time, verify post-merge state, and remove merged worktrees immediately.
"""
